apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fastboopmos-public-tls
spec:
  secretName: fastboopmos-public-tls
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
    - fastboopmos.samcday.com
