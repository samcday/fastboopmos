id: pmos-{{ release_name }}-{{ ui_name }}-{{ target_name }}
display_name: postmarketOS {{ release_name }} {{ ui_name }} {{ target_name }}
rootfs:
  ext4:
    mbr:
      index: 1
      android_sparseimg:
        xz:
          http: {{ image_url }}
          cors_safelisted_mode: true
          content:
            digest: sha512:{{ image_sha512 }}
            size_bytes: {{ image_size }}
kernel:
  path: /vmlinuz
  ext4:
    mbr:
      index: 0
      android_sparseimg:
        xz:
          http: {{ image_url }}
          cors_safelisted_mode: true
          content:
            digest: sha512:{{ image_sha512 }}
            size_bytes: {{ image_size }}
dtbs:
  path: /dtbs
  ext4:
    mbr:
      index: 0
      android_sparseimg:
        xz:
          http: {{ image_url }}
          cors_safelisted_mode: true
          content:
            digest: sha512:{{ image_sha512 }}
            size_bytes: {{ image_size }}
stage0:
  devices:
    samsung-gt510: {}
